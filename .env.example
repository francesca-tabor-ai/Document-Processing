# OptiFlowAI — copy to .env.local and set values. Never commit .env.local.

# Database (Railway Postgres or any PostgreSQL URL)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# AI providers (at least one required for document processing)
OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional: Anthropic for future use
# ANTHROPIC_API_KEY=
